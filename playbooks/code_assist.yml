version: 1
name: code-assist-gated
steps:
  - id: rust-gates
    run: |
      cargo fmt --all -- --check
      cargo clippy --all-targets -- -D warnings
  - id: ai-assist
    run: "./target/release/hauski-cli assist --playbook playbooks/ai_assist.yml --yes"
