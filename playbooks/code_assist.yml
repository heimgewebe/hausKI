version: 1
name: code-assist-gated
steps:
- id: lint
  run: |
    if [ -x ./scripts/wgx ]; then WGX=./scripts/wgx; else WGX=wgx; fi
    $WGX lint --export tmp/metrics.json
- id: ai-assist
  needs: [lint]
  run: "hauski assist --playbook code_review --metrics tmp/metrics.json"
