[package]
name = "heimlern-core"
version = "0.0.1"
edition = "2021"
license = "MIT"
description = "Stub heimlern-core types for policy API"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
