# Build artefacts
target/
.venv/
.uv/

# Agent-Kit sync (local virtualenvs & caches)
agent-kit/.venv/
agent-kit/.uv/
agent-kit/.pytest_cache/
agent-kit/.mypy_cache/

# Python caches
__pycache__/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.python-version

# Logs and misc
.env
configs/.env
.direnv/
**/*.log
*.log
.DS_Store
.coverage
coverage.xml

# Local caches (ignored by default; README is explicitly tracked below)
.local/*
!.local/README.md

# Modelle (lokal gehalten)
models/

# Project specific
.wgx/profile.local.yml
# Vendored crates are intentionally tracked to guarantee offline builds.
# Do not ignore vendor/.
# NICHT .vscode/settings.json ignorieren – projektweite Settings sind beabsichtigt

# MkDocs build output
site/

# Node (falls vorhanden)
node_modules/

# Tooling
observability/k6/summary.json
.lycheecache
.hauski-reports
artifacts/
